#include "bits/stdc++.h"

using namespace std;

long B , N , M;
int stone[500005];

int main(){
	
	cin >> B >> N >> M;

	
	for(int i = 0 ; i < M ; i++){
		cin >> stone[i]; 
	}
	
	int L = 0, R = L;
	
	while(L <= R){
		
		int mid = (L+R) / 2;
		if
		
	}
	
	
	
	
	return 0;
}
